#ifndef VARIABLE_HPP
#define VARIABLE_HPP

class variable
{
private:
	
public:
	variable();
	variable(variable const & src);
	~variable();

	variable & operator=(variable const & rhs);

	//get function
	//set function

};
#endif